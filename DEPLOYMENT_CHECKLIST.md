# 🚀 Deployment Checklist - PulseTrack MVP

**Дата проверки:** 2025-01-XX  
**Статус:** ⚠️ **Частично готово к деплою**

---

## ✅ Что уже готово

### Frontend (Flutter)
- ✅ Код компилируется без ошибок
- ✅ APK можно собрать (`flutter build apk --release`)
- ✅ BLE интеграция работает
- ✅ UI полностью реализован
- ✅ Алгоритмы расчета метрик работают
- ✅ Backend API интеграция реализована

### Backend (Node.js)
- ✅ Express сервер настроен
- ✅ PostgreSQL интеграция (TypeORM)
- ✅ JWT аутентификация
- ✅ REST API endpoints работают
- ✅ CORS настроен
- ✅ Helmet для безопасности

---

## ⚠️ Что нужно сделать перед деплоем

### 🔴 Критично (обязательно)

#### 1. Backend Production Configuration

**Проблема:** `synchronize: true` в `data-source.ts` - опасно для production!

**Решение:**
```typescript
// backend/src/infra/data-source.ts
synchronize: process.env.NODE_ENV !== 'production', // false в production
migrations: ['dist/migrations/*.js'], // использовать migrations
```

**Действия:**
- [ ] Создать database migrations вместо `synchronize: true`
- [ ] Настроить `.env` для production с реальными credentials
- [ ] Изменить `JWT_SECRET` на сильный случайный ключ
- [ ] Настроить `NODE_ENV=production`

#### 2. API Endpoint Configuration

**Проблема:** Hardcoded IP в `api_client.dart` (`172.20.10.2:4000`)

**Решение:**
```dart
// whoop_app/lib/core/api_client.dart
// Использовать environment variables или build config
baseUrl = const String.fromEnvironment(
  'API_BASE_URL',
  defaultValue: 'https://api.pulsetrack.com', // production URL
);
```

**Действия:**
- [ ] Создать `build_config.dart` для разных environments
- [ ] Настроить production API URL
- [ ] Убрать hardcoded IP адреса

#### 3. Database Migrations

**Текущее состояние:** `synchronize: true` автоматически создает/изменяет схему

**Нужно:**
- [ ] Создать TypeORM migrations для всех entities
- [ ] Протестировать migrations на чистой базе
- [ ] Документировать процесс миграции

#### 4. Environment Variables

**Создать `.env.production`:**
```env
NODE_ENV=production
PORT=4000
DB_HOST=your-production-db-host
DB_PORT=5432
DB_USER=your-db-user
DB_PASSWORD=strong-password-here
DB_NAME=pulsetrack_prod
JWT_SECRET=very-strong-random-secret-key-min-32-chars
CORS_ORIGIN=https://your-frontend-domain.com
```

**Действия:**
- [ ] Создать `.env.example` с примером (без реальных значений)
- [ ] Настроить production `.env` на сервере
- [ ] Добавить `.env` в `.gitignore` (если еще не добавлен)

#### 5. Security Hardening

**Проверить:**
- [ ] Helmet middleware включен ✅ (уже есть)
- [ ] CORS настроен правильно для production домена
- [ ] Rate limiting добавлен (рекомендуется)
- [ ] Input validation на всех endpoints
- [ ] SQL injection защита (TypeORM уже защищает)
- [ ] XSS защита

---

### 🟡 Важно (рекомендуется)

#### 6. Error Handling & Logging

**Нужно:**
- [ ] Production logging (Winston, Pino, или встроенный)
- [ ] Error tracking (Sentry, Rollbar, или аналог)
- [ ] Graceful error handling на всех endpoints
- [ ] Не показывать stack traces в production

#### 7. Database Backup

**Настроить:**
- [ ] Автоматические бэкапы PostgreSQL
- [ ] Retention policy (сколько дней хранить)
- [ ] Процедура восстановления из бэкапа

#### 8. Monitoring & Health Checks

**Добавить:**
- [ ] Health check endpoint (`/health`)
- [ ] Database connection monitoring
- [ ] Uptime monitoring (UptimeRobot, Pingdom)
- [ ] Performance monitoring

#### 9. SSL/HTTPS

**Обязательно для production:**
- [ ] SSL сертификат (Let's Encrypt бесплатно)
- [ ] HTTPS на backend API
- [ ] HTTPS на frontend (если web версия)
- [ ] Redirect HTTP → HTTPS

---

### 🟢 Желательно (можно позже)

#### 10. CI/CD Pipeline

**Настроить:**
- [ ] GitHub Actions / GitLab CI для автоматического деплоя
- [ ] Автоматические тесты перед деплоем
- [ ] Автоматический build APK при push

#### 11. Documentation

**Создать:**
- [ ] API documentation (Swagger/OpenAPI)
- [ ] Deployment guide
- [ ] Troubleshooting guide
- [ ] User manual

#### 12. Performance Optimization

**Проверить:**
- [ ] Database indexes на часто используемых полях
- [ ] Query optimization
- [ ] Caching стратегия (Redis опционально)
- [ ] CDN для статических файлов (если есть)

---

## 📋 План деплоя

### Вариант 1: Быстрый деплой (MVP для тестирования)

**Для:** Внутреннее тестирование, демо, beta тестирование

**Шаги:**
1. ✅ Собрать APK: `flutter build apk --release`
2. ⚠️ Настроить backend на VPS (DigitalOcean, AWS, Hetzner)
3. ⚠️ Создать PostgreSQL базу (managed или на том же VPS)
4. ⚠️ Обновить API URL в Flutter app
5. ⚠️ Пересобрать APK с новым URL
6. ✅ Распространить APK тестерам

**Время:** 2-4 часа  
**Стоимость:** $5-20/месяц (VPS + database)

### Вариант 2: Production деплой (для реальных пользователей)

**Для:** Публичный релиз, реальные пользователи

**Шаги:**
1. ✅ Выполнить все пункты из "Критично"
2. ✅ Настроить production сервер
3. ✅ Настроить SSL/HTTPS
4. ✅ Настроить мониторинг
5. ✅ Протестировать на staging окружении
6. ✅ Создать production build
7. ✅ Деплой на production
8. ✅ Мониторинг после деплоя

**Время:** 1-2 дня  
**Стоимость:** $20-100/месяц (зависит от нагрузки)

---

## 🎯 Рекомендации по деплою

### Для MVP/Тестирования (сейчас можно)

**✅ Можно деплоить если:**
- Используете для внутреннего тестирования
- Небольшая группа тестеров (5-20 человек)
- Готовы быстро исправлять проблемы
- Backend на защищенном VPS с базовым firewall

**Минимальные требования:**
1. Backend на VPS с PostgreSQL
2. Обновить API URL в Flutter app
3. Собрать release APK
4. Распространить APK

**⚠️ Ограничения:**
- `synchronize: true` все еще включен (риск потери данных при изменениях)
- Hardcoded IP нужно заменить
- Нет автоматических бэкапов
- Нет мониторинга

### Для Production (нужно доработать)

**❌ Не рекомендуется деплоить пока:**
- Не созданы database migrations
- `synchronize: true` не отключен
- Нет production environment configuration
- Нет SSL/HTTPS
- Нет мониторинга и логирования

---

## 🛠️ Быстрые исправления для деплоя

### 1. Исправить API Client (5 минут)

```dart
// whoop_app/lib/core/api_client.dart
class ApiClient {
  ApiClient._internal() {
    // Использовать const для production или environment variable
    const String baseUrl = String.fromEnvironment(
      'API_BASE_URL',
      defaultValue: 'https://api.pulsetrack.com', // или ваш production URL
    );
    
    _dio = Dio(BaseOptions(
      baseUrl: baseUrl,
      // ... остальное
    ));
  }
}
```

**Сборка с production URL:**
```bash
flutter build apk --release --dart-define=API_BASE_URL=https://api.pulsetrack.com
```

### 2. Исправить Database Config (10 минут)

```typescript
// backend/src/infra/data-source.ts
export const AppDataSource = new DataSource({
  // ... остальное
  synchronize: process.env.NODE_ENV !== 'production',
  // В production использовать migrations
  migrations: process.env.NODE_ENV === 'production' 
    ? ['dist/migrations/*.js'] 
    : [],
  logging: process.env.NODE_ENV === 'development',
});
```

### 3. Создать .env.example (2 минуты)

```env
# .env.example
NODE_ENV=development
PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=pulsetrack
JWT_SECRET=your-secret-key-change-in-production
CORS_ORIGIN=http://localhost:3000
```

---

## 📊 Готовность к деплою

| Компонент | Статус | Готовность |
|-----------|--------|------------|
| **Frontend Code** | ✅ Готов | 100% |
| **Backend Code** | ✅ Готов | 100% |
| **API Integration** | ✅ Готов | 100% |
| **Database Schema** | ⚠️ Нужны migrations | 70% |
| **Security** | ⚠️ Базовая | 60% |
| **Configuration** | ❌ Нужна настройка | 40% |
| **Monitoring** | ❌ Отсутствует | 0% |
| **Documentation** | ⚠️ Частичная | 50% |

**Общая готовность: ~65%**

---

## ✅ Вывод

### Можно ли деплоить сейчас?

**Для MVP/Тестирования:** ✅ **ДА, с минимальными исправлениями**

**Что нужно сделать (30-60 минут):**
1. Настроить backend на VPS
2. Обновить API URL в Flutter app
3. Собрать release APK
4. Распространить тестерам

**Для Production:** ❌ **НЕТ, нужно доработать**

**Что нужно сделать (1-2 дня):**
1. Создать database migrations
2. Настроить production environment
3. Добавить SSL/HTTPS
4. Настроить мониторинг
5. Протестировать на staging

---

## 🚀 Следующие шаги

1. **Если нужен быстрый деплой для тестирования:**
   - Выполнить "Быстрые исправления" выше
   - Настроить VPS
   - Собрать и распространить APK

2. **Если готовитесь к production:**
   - Выполнить все пункты из "Критично"
   - Настроить staging окружение
   - Протестировать
   - Затем деплой на production

---

**Рекомендация:** Начните с MVP деплоя для тестирования, затем доработайте для production.
