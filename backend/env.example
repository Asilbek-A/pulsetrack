# PulseTrack Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!
#
# Usage: cp env.example .env
# Then edit .env with your actual values

# Environment
NODE_ENV=development
PORT=4000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=pulsetrack

# JWT Secret (IMPORTANT: Use a strong random string in production!)
# Generate with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars

# CORS Origin (comma-separated for multiple origins)
# For development: *
# For production: https://your-frontend-domain.com,https://app.pulsetrack.com
CORS_ORIGIN=*
